spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/evolution_db
    username: ren1kron
    password: ren1kron
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  servlet:
    context-path: /app